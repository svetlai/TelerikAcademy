//Ignore below this line. It's roughly written code for a better presentation of the homework.
//-------------------------------------Preview tasks 1 & 2--------------------------------------------
var literal = intLit;

function displayLitInConsole(literal) {
    console.log(literal); 				//Display in Console
    jsConsole.writeLine(literal); 		//Display in Inline Console
}

//-------------------------------------Preview tasks 3 & 4--------------------------------------------
function displayTypeOf(literal) {
    console.log(typeof (literal));
    jsConsole.writeLine(typeof (literal));
}

function clearConsole() {
    jsConsole.clear();
}